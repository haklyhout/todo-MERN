* Frontend :
  - command "cd frontend"
  - first run "npm install" to install all packages
  - run project with "npm start"
 
* Backend :
  - command "cd backend"
  - first run "npm install" to install all packages
  - run project with "npm start"
